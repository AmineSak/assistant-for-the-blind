# Create project directory if not already created
mkdir -p ~/environment_system

# Save the Python script
cd ~/environment_system
nano environment_description.py
# [Paste the full code from the Environment Description System artifact]
# Save with Ctrl+O, Exit with Ctrl+X

# Make the script executable
chmod +x environment_description.py